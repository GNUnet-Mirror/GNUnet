if(WITHOUT_MANPAGES)
	if(WITH_DOCUMENTATION)
		add_subdirectory(tutorial)
		add_subdirectory(handbook)
	endif()
else (WITHOUT_MANPAGES)
	add_subdirectory(man)
	if(WITH_DOCUMENTATION)
		add_subdirectory(tutorial)
		add_subdirectory(handbook)
	endif()
endif()
