@INLINE@ template_dv.conf

[testbed]
SETUP_TIMEOUT = 60 m
OPERATION_TIMEOUT = 30 m

[transport]
PORT = 2565
PLUGINS = tcp dv
#PREFIX = valgrind --leak-check=full --track-fds=yes --leak-resolution=high

[dv]
AUTOSTART = YES
# PREFIX = valgrind --leak-check=full --track-fds=yes --leak-resolution=high

[set]
AUTOSTART = YES
# PREFIX = valgrind --leak-check=full --track-fds=yes --leak-resolution=high

[core]
# PREFIX = valgrind --leak-check=full --track-fds=yes --leak-resolution=high

